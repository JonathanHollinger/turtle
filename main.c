#include <stdio.h>
#include "enums.h"
#include "shell.h"

int main(int argc, char *argv[]) {
  printf("Hello world!\n");

  // Code to be executed. I want to add flags later but that's for later.
  shell();

  return EXIT_SUCCESS;
}