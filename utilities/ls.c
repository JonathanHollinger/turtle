#include <stdio.h>
#include <stdlib.h>

//Should be main, need to fix build path potentially
int m(int argc, char *argv[]) {
    printf("ls is now executing!\n");
      // Code to be executed
      return EXIT_SUCCESS;
}